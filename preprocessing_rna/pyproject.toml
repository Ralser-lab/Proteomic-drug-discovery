[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "protacs"
version = "0.1.0"

[tool.hatch.build.targets.wheel]
packages = ["src/gsea", "src/preprocessing", "src/viz", "src/file_io"]
